requests>=2.33,<3

# LLM providers; only the configured provider is used at runtime.
# Structured output support requires a recent Anthropic SDK.
anthropic>=0.75,<1
openai>=1.50,<2
groq>=0.13,<1

pytest>=9.0.3,<10
pytest-cov>=7,<8
