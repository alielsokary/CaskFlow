# Core
requests>=2.32,<3
beautifulsoup4>=4.12,<5

# LLM providers (only the configured one is actually used at runtime)
# anthropic floor: output_config (structured outputs) needs a 2026-era SDK
anthropic>=0.75,<1
openai>=1.50,<2
groq>=0.13,<1
# Cloudflare Workers AI uses requests directly — no SDK needed

# Dev / test
pytest>=8.0,<9
